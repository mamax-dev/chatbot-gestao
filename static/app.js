const form=document.querySelector('#form'),input=document.querySelector('#question'),button=document.querySelector('#send'),chat=document.querySelector('#chat');function add(text,type){const el=document.createElement('div');el.className=`message ${type}`;el.textContent=text;chat.appendChild(el);chat.scrollTop=chat.scrollHeight;return el}form.addEventListener('submit',async e=>{e.preventDefault();const question=input.value.trim();if(!question)return;add(question,'user');input.value='';button.disabled=true;const wait=add('Consultando o documento...','bot');try{const r=await fetch('/api/perguntar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question})});const d=await r.json();wait.textContent=r.ok?d.answer:(d.detail||'Erro ao processar.')}catch{wait.textContent='Não foi possível conectar ao servidor.'}finally{button.disabled=false;input.focus()}});
