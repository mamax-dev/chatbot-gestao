# Aplicação final auditada

1. Preserve `static/hero-support.mp4` do repositório atual.
2. Substitua os demais arquivos pelas versões deste pacote.
3. Configure as variáveis do `render.yaml` no Render.
4. Execute `pytest -q`.
5. Faça `Clear build cache & deploy`.

A interface original recebida foi preservada sem alterações. O backend usa `data/empresa.json`, FAQ local, cache em memória, Gemini complementar, Telegram, proteção contra ruído e transferência simulada.
